# Nomad API Configuration
NOMAD_ADDR=http://127.0.0.1:4646
# NOMAD_TOKEN=

# MCP: default binary transport is **stdio**. For HTTP Inspector use flags, not env:
#   go run . -transport=streamable-http -port=8080   # URL http://localhost:8080/mcp
#   go run . -transport=sse -port=8080               # SSE path /sse
